"""
Forge-OH in-repo extensions to the OpenHands SDK's tool surface.

Modules under this package expose additional tools that the agent can call
via the OpenHands plugin/skills registration path. Nothing here is copied
from the OpenHands upstream tree; the modules integrate into it at runtime
(see `openhands_tools_ext/repograph/plugin.py` when it lands in slice D.5).
"""
