# Forge-OH BFF runtime dependencies
# Clean runtime set validated separately from the full local lock snapshot.
# Full environment snapshot remains in bff/requirements.lock.

# Web framework
fastapi==0.115.5
uvicorn==0.39.0
pydantic==2.12.5
pydantic-settings==2.6.1

# OpenHands SDK (aligned with .oh-venv from Step 1)
openhands-sdk==1.40.0
openhands-tools==1.40.0
openhands-agent-server==1.40.0
openhands-workspace==1.40.0

# HTTP client (loose pin — openhands 1.40.x has its own upper bound)
httpx>=0.28.1

# WebSocket / Socket.IO (aiohttp pin removed — openhands-tools/browser-use requires >=3.13.3)
python-socketio>=5.11.4
aiohttp>=3.13.3

# Database
aiosqlite==0.22.1

# Slice D — RepoGraph (Rec #1)
# Neo4j driver for the code knowledge graph (DozerDB 5.26.27 on Colossus)
neo4j>=5.26,<6
# Actively maintained replacement for tree_sitter_languages
tree-sitter>=0.23,<1
tree-sitter-language-pack>=0.4.0,<1

# Stage 5.2 — VectorPort backend (RealQdrantBackend wraps AsyncQdrantClient).
# Lazy-imported inside RealQdrantBackend.__init__; contract tests use the
# InMemoryQdrantBackend fake and do NOT require this dep to pass.
qdrant-client>=1.12,<2

# Optional dev/test
# pytest==8.3.4
# pytest-asyncio==0.24.0
