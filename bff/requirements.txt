# Forge-OH BFF dependencies
# Keep versions pinned; update intentionally.

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1

# OpenHands SDK
# Version 1.29.3 is the canonical pinned version for this project.
openhands-ai==1.29.3

# HTTP client (for proxying requests to OpenHands SDK)
httpx==0.28.1

# WebSocket / Socket.IO
python-socketio==5.11.4
aiohttp==3.11.10

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Database (episodic memory store)
aiosqlite==0.20.0

# Observability
opentelemetry-sdk==1.29.0
opentelemetry-exporter-otlp-proto-http==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0

# Dev / test (not installed in production)
# pytest==8.3.4
# pytest-asyncio==0.24.0
# httpx  (already above, used in tests too)
