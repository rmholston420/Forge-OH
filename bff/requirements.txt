# Forge-OH BFF dependencies
# Pinned to versions that are actually available in this environment.
# Keep OTEL unpinned here because openhands-sdk pulls a compatible set.

# Web framework
fastapi==0.115.5
pydantic==2.12.5
pydantic-settings==2.6.1

# OpenHands SDK
openhands-sdk==1.29.3

# HTTP client
httpx==0.28.1

# WebSocket / Socket.IO
python-socketio==5.11.4
aiohttp==3.11.10

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Database
aiosqlite==0.22.1

# Dev / test
# pytest==8.3.4
# pytest-asyncio==0.24.0

# Synced from working .venv on local machine
uvicorn==0.39.0
vllm==0.25.0
opentelemetry-exporter-otlp==1.43.0
opentelemetry-exporter-otlp-proto-grpc==1.39.1
opentelemetry-exporter-otlp-proto-http==1.39.1
prometheus-fastapi-instrumentator==8.0.2
